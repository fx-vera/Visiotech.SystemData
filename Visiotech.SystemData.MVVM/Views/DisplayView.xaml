<UserControl x:Class="Visiotech.SystemData.MVVM.Views.DisplayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Visiotech.SystemData.MVVM.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" MinHeight="100"></RowDefinition>
            <RowDefinition Height="auto" MinHeight="100"></RowDefinition>
        </Grid.RowDefinitions>
        <DataGrid Margin="5" Grid.Row="0"></DataGrid>
        <Grid Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Label Content="Interval"></Label>
                <!--Here the numeric up down (maybe host from winforms)-->
            </StackPanel>
            <DockPanel Grid.Column="1">
                <Button Content="Start" Command="{Binding StartCommand}"></Button>
                <Button Content="Pause" Command="{Binding PauseCommand}"></Button>
            </DockPanel>
        </Grid>
    </Grid>
</UserControl>
